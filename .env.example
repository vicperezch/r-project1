# Copy to .env. Docker compose reads this file directly, which matters here
# because sudo strips environment variables, so exporting the key in your shell
# would not reach the containers.
ANTHROPIC_API_KEY=sk-ant-...
CHATBOT_MODEL=claude-haiku-4-5
